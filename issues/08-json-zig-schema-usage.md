# `json.zig`'s `Schema` usage

32 dead lines in `json.zig`, used via `@import("../json.zig").Schema` in
a few `linter/config/*.zig` files of `vendor/zlint`. Not yet
investigated — could be another instance of issue 1, 5, or 6, or
something new.
